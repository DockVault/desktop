# Dependencies
node_modules/

# Build / packaging output (but keep the committed app-icon source under build/)
dist/
out/
build/*
!build/icon.png
release/
*.asar

# Local-only runtime data, scratch, captured evidence — never committed
.local/

# Environment / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Tooling / scan artifacts (stray local state, never committed)
.semgrep/
